import { component$ } from '@builder.io/qwik';

export const Hero = component$(() => {
  return (
    <section>
      <div class="site-container py-4">
        <h1>Hello World</h1>
      </div>
    </section>
  );
});

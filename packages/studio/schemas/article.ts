import { SchemaTypeDefinition } from 'sanity';

export default {
  name: 'article',
  title: 'Article',
  type: 'document',
  fields: [
    {
      name: 'title',
      title: 'Title',
      type: 'string',
    },
    {
      name: 'slug',
      title: 'Slug',
      type: 'slug',
    },
  ],
} satisfies SchemaTypeDefinition;
